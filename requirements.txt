# Target interpreter: Python 3.12 (see .python-version).
# EconML and scikit-learn ship wheels on 3.12; Python 3.14 often forces source builds and breaks pip install.

numpy>=1.24
pandas>=2.0
scipy
scikit-learn>=1.3,<1.7
econml>=0.15
statsmodels
dowhy
networkx==2.8.8
# pydot 4.x breaks nx.nx_pydot.from_pydot with networkx 2.8 (get_strict); dowhy 0.12 also expects nx 2.x (d_separated API)
pydot>=1.4.2,<3
plotly
streamlit
matplotlib
tqdm
geopandas
pydot
seaborn
joblib>=1.3
